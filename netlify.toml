[build]
  # The directory where Netlify will look for your client code
  base = "client"
  
  # The command to build your frontend
  command = "npm run build"
  
  # The directory where Vite outputs your built files
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
